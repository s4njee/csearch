apiVersion: batch/v1
kind: Job
metadata:
  name: db-migrate
  labels:
    app.kubernetes.io/name: db-migrate
    app.kubernetes.io/component: migration
    app.kubernetes.io/part-of: csearch
  annotations:
    # Runs on every sync, after the StatefulSet is up. migrate.py is idempotent
    # (applies pending migrations; no-op when current), so this is the single
    # schema mechanism for bootstrap AND ongoing change — see db/README.md.
    argocd.argoproj.io/hook: Sync
    argocd.argoproj.io/hook-delete-policy: BeforeHookCreation,HookSucceeded
spec:
  backoffLimit: 3
  template:
    metadata:
      labels:
        app.kubernetes.io/name: db-migrate
        app.kubernetes.io/component: migration
        app.kubernetes.io/part-of: csearch
    spec:
      restartPolicy: OnFailure
      imagePullSecrets:
        - name: registry-s8njee-pull
      securityContext:
        fsGroup: 999
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: migrate
          image: registry.s8njee.com/csearch-db-migrate:latest
          imagePullPolicy: Always
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            runAsUser: 999
            runAsGroup: 999
            capabilities:
              drop:
                - ALL
          envFrom:
            - configMapRef:
                name: postgres-config
            - secretRef:
                name: postgres-auth
          command:
            - /bin/sh
            - -c
            - |
              set -eu
              export PGHOST="$POSTGRES_RW_HOST"
              export PGUSER="$POSTGRES_USER"
              export PGDATABASE="$POSTGRES_DB"
              export PGPASSWORD="$POSTGRES_PASSWORD"
              until pg_isready -h "$PGHOST" -U "$PGUSER" -d "$PGDATABASE"; do
                echo "waiting for postgres..."; sleep 2
              done
              python3 /db/migrate.py
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
