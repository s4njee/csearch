apiVersion: v1
kind: Secret
metadata:
  name: postgres-auth
  labels:
    app.kubernetes.io/name: postgres
    app.kubernetes.io/component: database
# CNPG bootstrap expects kubernetes.io/basic-auth with username + password keys.
type: kubernetes.io/basic-auth
stringData:
  username: postgres
  password: ${POSTGRES_PASSWORD}
