# Kubernetes Archive

Legacy manifests are not kept in the active tree.

Use the Argo-managed roots under `k8s/netcup-*` and `k8s/freya-*` for current
cluster changes. If an older manifest is needed for archaeology, recover it
from git history rather than treating an archived copy as deployable source.
