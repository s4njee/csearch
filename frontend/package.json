{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "node scripts/write-runtime-config.mjs",
    "pregenerate": "node scripts/write-runtime-config.mjs",
    "build": "nuxt build",
    "dev": "nuxt dev --host",
    "generate": "nuxt generate",
    "pages:deploy": "npm run generate && npx wrangler pages deploy .output/public --project-name \"${CF_PAGES_PROJECT:-csearch}\"",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@nuxt/devtools": "3.2.3",
    "nuxt": "^4.4.8",
    "vue": "^3.5.0",
    "vue-router": "^4.5.0"
  },
  "dependencies": {
    "@nuxt/ui": "^4.9.0",
    "@types/plotly.js": "^3.0.10",
    "plotly.js-dist-min": "^3.4.0"
  }
}
