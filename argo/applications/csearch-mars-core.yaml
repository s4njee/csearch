apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: csearch-mars-core
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  project: default
  source:
    repoURL: https://github.com/s4njee/csearch.git
    targetRevision: mars
    path: k8s/mars-core
  destination:
    server: https://kubernetes.default.svc
    namespace: csearch
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
